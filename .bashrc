# Executes on every command prompt
# to append current session history to history file
PROMPT_COMMAND='history -a'

# for setting history length see HISTSIZE and HISTFILESIZE in bash(1)
HISTSIZE=5000
HISTFILESIZE=20000
# add timestamp to history output
HISTTIMEFORMAT="%F %T "
