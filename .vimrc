"Soft tabs (spaces) need all 3 options below
set expandtab
set shiftwidth=4
set softtabstop=4

"Set indent of newline same as previous line
set autoindent

set showcmd

"Start searching and highlight closest match immediately
set incsearch

"Keep searching parent directories for tags file (e.g. made by ctags) 
set tags=./tags;
